<Window x:Class="MajdataEdit.ErrorList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="ja"
        lex:ResxLocalizationProvider.DefaultAssembly="MajdataEdit"
        lex:ResxLocalizationProvider.DefaultDictionary="Langs"
        mc:Ignorable="d"
        Background="{DynamicResource WindowBackground}"
        Title="{lex:Loc Key=DetResult}" Height="406" Width="500" ResizeMode="NoResize" WindowStartupLocation="Manual">

    <Window.Resources>
        <Style x:Key="HeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="{Binding Mode=OneWay, Source={StaticResource WindowBackground}}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="ItemStyle" TargetType="DataGridCell">
            <Setter Property="Background" Value="{Binding Mode=OneWay, Source={StaticResource ButtonsBackground}}"/>
            <Style.Triggers>

            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        
        <DataGrid x:Name="ErrorListView" Grid.Row="0" Margin="-8,0,0,0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Disabled" 
                  Background="{Binding Mode=OneWay, Source={StaticResource WindowBackground}}" 
                  CanUserResizeColumns="False" CanUserSortColumns="False" CanUserReorderColumns="False" AutoGenerateColumns="False" IsReadOnly="True" 
                  CellStyle="{Binding Mode=OneWay, Source={StaticResource ItemStyle}}" 
                  SelectionChanged="ErrorListView_SelectionChanged" MouseDoubleClick="ErrorListView_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn CanUserResize="False" CanUserReorder="False" IsReadOnly="True" Visibility="Visible" Width="50" Binding="{Binding Type}" HeaderStyle="{Binding Mode=OneWay, Source={StaticResource HeaderStyle}}"/>
                <DataGridTextColumn CanUserResize="False" CanUserReorder="False" IsReadOnly="True" Visibility="Visible" Width="60" Header="{lex:Loc Key=Position}" Binding="{Binding Position}" HeaderStyle="{Binding Mode=OneWay, Source={StaticResource HeaderStyle}}"/>
                <DataGridTextColumn CanUserResize="False" CanUserReorder="False" IsReadOnly="True" Visibility="Visible" Width="*" Header="{lex:Loc Key=Message}" Binding="{Binding Message}" HeaderStyle="{Binding Mode=OneWay, Source={StaticResource HeaderStyle}}"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBox x:Name="txtDetail" Grid.Row="1" TextWrapping="Wrap" Background="{Binding Mode=OneWay, Source={StaticResource ButtonsBackground}}" Foreground="#FFCFCDC5" IsReadOnly="True"/>
    </Grid>
</Window>
