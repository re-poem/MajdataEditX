<Window x:Class="MajdataEdit.LaunchMaiMuriDX"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture="zh-CN"
        lex:ResxLocalizationProvider.DefaultAssembly="MajdataEdit"
        lex:ResxLocalizationProvider.DefaultDictionary="Langs"
        mc:Ignorable="d"
        Background="{DynamicResource WindowBackground}"
        Title="{lex:Loc Key=LaunchMaiMuriDX}" Height="75" Width="274" ResizeMode="NoResize" Loaded="Window_Loaded"
        WindowStartupLocation="CenterOwner" Initialized="Window_Initialized">
    <Window.Resources>

    </Window.Resources>


    <Grid Margin="8,5,8,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="1" Orientation="Horizontal">
            <CheckBox x:Name="RenderEnable_Checkbox" Margin="0,5,0,0"
                      IsChecked="False"
                      FontWeight="Bold" FontSize="13"
                      Foreground="{DynamicResource ButtonForeground}"
                      Content="{lex:Loc Key=RenderEnable}" />
        </StackPanel>
        <StackPanel Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="1" Orientation="Horizontal">
            <Button x:Name="StartCheck_Button" Content="{lex:Loc Key=StartCheck}" Click="StartCheck_Button_Click"
                Width="100" Height="28" Margin="10,0,0,0"
                Foreground="{DynamicResource ButtonForeground}"
                Background="{DynamicResource ButtonsBackground}" />
        </StackPanel>
        <Label Foreground="#999999" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" FontSize="9" Content="powered by Minepig"/>
    </Grid>

</Window>